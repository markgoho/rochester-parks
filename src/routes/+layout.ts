export const prerender = true;
export const trailingSlash = 'always';
// The site has no interactivity. Without client JS, links do full page loads,
// so the cross-document view transition in the head still works (like Hugo).
export const csr = false;
